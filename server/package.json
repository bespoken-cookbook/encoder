{
  "name": "alexa-encoder",
  "version": "0.0.1",
  "devDependencies": {
    "coveralls": "^2.11.14",
    "mocha": "^3.1.0",
    "mocha-lcov-reporter": "^1.2.0"
  },
  "scripts": {
    "pretest": "tsc -p . && cp -rf test/assets outputs/source/test/ && rm -r ./outputs/reports/coverage",
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha ./outputs/source/test || mv ./coverage ./outputs/reports"
  }
}
